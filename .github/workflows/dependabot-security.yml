# Security controls for Dependabot PRs
# Prevents auto-merge and adds security checks for npm updates
name: Dependabot Security Controls

on:
  pull_request:
    branches:
      - main

jobs:
  dependabot-security:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Check if npm dependency update
        id: check_npm
        run: |
          if git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -q "package"; then
            echo "is_npm=true" >> $GITHUB_OUTPUT
            echo "::warning::This is an npm dependency update - requires manual security review"
          else
            echo "is_npm=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Block auto-merge for npm updates
        if: steps.check_npm.outputs.is_npm == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ **NPM Dependency Update - Manual Review Required**\n\n' +
                    'This PR updates npm dependencies. Due to recent npm supply chain attacks, ' +
                    'this PR requires manual security review before merging.\n\n' +
                    '**Security Checklist:**\n' +
                    '- [ ] Review the changed packages for known vulnerabilities\n' +
                    '- [ ] Check for suspicious postinstall scripts\n' +
                    '- [ ] Verify package authenticity and maintainer reputation\n' +
                    '- [ ] Run `./utils/scan-npm-compromise.sh` locally\n' +
                    '- [ ] Run `npm audit` and review output\n' +
                    '- [ ] Build library and inspect bundle for unexpected code\n' +
                    '- [ ] Review changelog of updated packages\n\n' +
                    '**Do not enable auto-merge for this PR.**\n\n' +
                    'See [docs/SECURITY-NPM-SUPPLY-CHAIN.md](../blob/main/docs/SECURITY-NPM-SUPPLY-CHAIN.md) for detailed security procedures.'
            });
            
            // Remove auto-merge if it was enabled
            try {
              await github.rest.pulls.deletePullRequestReviewProtection({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.issue.number
              });
            } catch (error) {
              console.log('No auto-merge to disable');
            }
      
      - name: Setup Node.js
        if: steps.check_npm.outputs.is_npm == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Run npm audit
        if: steps.check_npm.outputs.is_npm == 'true'
        run: |
          npm audit --production || echo "::warning::npm audit found vulnerabilities"
          
      - name: Comment audit results
        if: steps.check_npm.outputs.is_npm == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const { execSync } = require('child_process');
            try {
              const auditOutput = execSync('npm audit --json', { encoding: 'utf-8' });
              const audit = JSON.parse(auditOutput);
              const criticalCount = audit.metadata.vulnerabilities.critical || 0;
              const highCount = audit.metadata.vulnerabilities.high || 0;
              
              if (criticalCount > 0 || highCount > 0) {
                github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: `🚨 **npm audit found ${criticalCount} critical and ${highCount} high severity vulnerabilities!**\n\n` +
                        `Run \`npm audit\` for details.`
                });
              } else {
                github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: `✅ **npm audit:** No critical or high severity vulnerabilities found.\n\n` +
                        `Still requires manual security review before merging.`
                });
              }
            } catch (error) {
              console.log('npm audit check completed with warnings');
            }
